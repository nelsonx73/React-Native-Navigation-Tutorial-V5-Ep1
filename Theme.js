import { Dimensions } from "react-native";

const { width, height } = Dimensions.get("window");
const ICON_SIZE = 56;
const SPACING = 16;
const ITEM_WIDTH = width * 0.06;

export { width, height, ICON_SIZE, SPACING, ITEM_WIDTH };
